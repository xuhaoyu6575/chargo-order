# 复制为 cloud-credentials-secret.yaml 后填写真实值，切勿提交到 Git。
# kubectl apply -f cloud-credentials-secret.yaml -n robot-op-center
apiVersion: v1
kind: Secret
metadata:
  name: robot-op-cloud-credentials
  namespace: robot-op-center
type: Opaque
stringData:
  ak: "REPLACE_WITH_CLOUD_AK"
  sk: "REPLACE_WITH_CLOUD_SK"
